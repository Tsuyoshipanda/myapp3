  <% unless post.work_user(current_user.id).blank? %>
    <%= link_to post_work_path(post_id: post.id ,id: post.works[0].id), method: :delete, remote: true do %>
          <h3>Now Working</h3>
          <%= post.work_count %>

    <% end %>
  <% else %>
    <%= link_to post_works_path(post.id), method: :post, remote: true do %>
          <h3>Now Working</h3>
          <%= post.work_count %>
    <% end %>
  <% end %>
