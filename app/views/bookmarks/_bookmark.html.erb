<% if post.bookmarked_by?(current_user) %>
    <p><%=link_to "ブックマーク解除", post_bookmarks_path(post.id), method: :delete, remote: true %></p>
<% else %>
    <p><%=link_to "ブックマーク登録", post_bookmarks_path(post.id), method: :post, remote: true %></p>
<% end %>
